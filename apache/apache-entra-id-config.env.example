# Configuration values for Apache to connect directly to Entra ID (Microsoft Azure AD)
# Copy this file to apache-entra-id-config.env and fill in your values
# The .env file is gitignored and will not be committed

# Entra ID Tenant ID (found in Azure Portal > Azure Active Directory > Overview)
ENTRA_ID_TENANT_ID=your-tenant-id-here

# Entra ID Application (Client) ID (found in App Registration > Overview)
# This must be a different app registration than the one used for Keycloak
# The redirect URI must be: https://localhost:8443/redirect_uri
ENTRA_ID_CLIENT_ID=your-client-id-here

# Entra ID Client Secret (found in App Registration > Certificates & secrets)
ENTRA_ID_CLIENT_SECRET=your-client-secret-here

# Optional: OIDC Crypto Passphrase for session encryption
# If not set, defaults to: a-random-secret-key-change-this-in-production
# OIDC_CRYPTO_PASSPHRASE=your-crypto-passphrase-here

